# LuxeMarket

A small React + Vite storefront project styled with Tailwind CSS.

## Deploy
This repository is configured to deploy to GitHub Pages using `gh-pages`.

To deploy locally:

```bash
npm install
npm run build
npm run deploy
```

Make sure the `homepage` field in `package.json` matches your GitHub Pages URL.
